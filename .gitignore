# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Server runtime state — the legacy SQLite database, the pre-Cloudinary imagery
# and the session secret are all machine-local and must never be committed.
server/data
server/uploads

# Supabase and Cloudinary credentials.
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example
